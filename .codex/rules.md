# 项目执行规则

1. 本项目采用 venv 环境的 Python 执行，所有 Python 操作都用 uv 在本项目下执行；uv、pip 都使用国内源。
2. 写的代码前后端分离。
3. 代码文件与参数文件分离。
4. 代码必须给出函数级注释。
5. 将每一次对话的 QA 简单记录到项目根目录的 history.md，包含时间戳、用户需求、最终的主要修改或主要结论。
6. 全程使用 Git 管理，每一次重大更改都要 commit。
